#include "main.h"

/**
 * _abs - Processes the value of an integer.
 * @n: The integer to be processed.
 *
 * Return: The value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

